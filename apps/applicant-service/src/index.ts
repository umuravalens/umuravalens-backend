import cors from "cors";import express from "express";import morgan from "morgan";import { logger, ok } from "@umurava/shared-utils";import { connectDb } from "./config/db";import { env } from "./config/env";import { errorHandler, notFound } from "./middlewares/error";import applicantRoutes from "./routes/applicantRoutes";const app=express();app.use(cors());app.use(express.json());app.use(morgan("combined",{stream:{write:(message)=>logger.info({message:message.trim()})}}));app.get("/health",(_req,res)=>res.json(ok({service:"applicant-service",status:"up"})));app.use("/",applicantRoutes);app.use(notFound);app.use(errorHandler);connectDb(env.mongodbUri).then(()=>app.listen(env.port,()=>logger.info({message:`Applicant service running on port ${env.port}`}))).catch((error)=>{logger.error({message:"Failed to start applicant service",error});process.exit(1);});
