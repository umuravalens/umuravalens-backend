import { Schema, model } from "mongoose";interface ScreeningDocument{jobId:string;status:"pending"|"processing"|"completed";}const screeningSchema=new Schema<ScreeningDocument>({jobId:{type:String,required:true,index:true},status:{type:String,enum:["pending","processing","completed"],default:"pending"}},{timestamps:true});export const Screening=model<ScreeningDocument>("Screening",screeningSchema);
