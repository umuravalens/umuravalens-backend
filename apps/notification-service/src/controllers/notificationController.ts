import { NextFunction, Request, Response } from "express";import { AppError, ok } from "@umurava/shared-utils";import { Server } from "socket.io";export const createEventEmitter=(io:Server)=>{return(req:Request,res:Response,next:NextFunction)=>{try{const {event,payload}=req.body;if(!event) throw new AppError("event is required",400);io.emit(event,payload||{});res.json(ok({emitted:true,event}));}catch(error){next(error);}};};
